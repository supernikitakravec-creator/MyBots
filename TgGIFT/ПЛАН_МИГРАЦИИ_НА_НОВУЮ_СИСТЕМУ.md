# 🔄 ПЛАН МИГРАЦИИ НА НОВУЮ СИСТЕМУ ПОПОЛНЕНИЯ

## 📋 Текущее Состояние
- ❌ Система пополнения через подарки Stars не работает
- ❌ Нет интеграции с платежными системами
- ❌ Нет монетизации через комиссии
- ✅ Есть базовая система управления аккаунтом (Pyrogram)

## 🎯 Целевое Состояние (как у Portals Market)
- ✅ Интеграция с Split (без KYC) и Fragment (с KYC)
- ✅ Комиссия 10% с транзакций
- ✅ Профили автопокупки с гибкими настройками
- ✅ Покупка подарков от имени пользователя

## 📝 ПОШАГОВЫЙ ПЛАН МИГРАЦИИ

### Этап 1: Исследование API (1-2 дня)

#### 1.1 Split API
```bash
# Изучить документацию
https://split.com/api/docs

# Необходимые методы:
- Создание платежной ссылки
- Проверка статуса платежа
- Webhook для уведомлений
```

#### 1.2 Fragment API
```bash
# Изучить документацию
https://fragment.com/api

# Необходимые методы:
- Авторизация с KYC
- Создание платежа
- Проверка баланса
```

### Этап 2: Обновление Архитектуры (2-3 дня)

#### 2.1 Новые Файлы
```python
# payment_providers.py - интеграция с платежными системами
# user_profiles.py - управление профилями автопокупки
# mini_app.py - веб-интерфейс для настроек
```

#### 2.2 Обновление БД
```sql
-- Новые таблицы
CREATE TABLE payment_providers (
    id INTEGER PRIMARY KEY,
    user_id INTEGER,
    provider TEXT, -- 'split' или 'fragment'
    provider_user_id TEXT,
    is_verified BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE payment_transactions (
    id INTEGER PRIMARY KEY,
    user_id INTEGER,
    provider TEXT,
    amount REAL,
    commission REAL,
    status TEXT,
    provider_tx_id TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE user_autopurchase_profiles (
    id INTEGER PRIMARY KEY,
    user_id INTEGER,
    profile_name TEXT,
    min_stars INTEGER DEFAULT 100,
    max_stars INTEGER DEFAULT 1000,
    max_circulation INTEGER DEFAULT 500000,
    target_channel TEXT,
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### Этап 3: Реализация Платежной Системы (3-4 дня)

#### 3.1 Базовая Интеграция
```python
# payment_providers.py
import aiohttp
from typing import Dict, Optional

class PaymentProvider:
    def __init__(self, api_key: str):
        self.api_key = api_key
        
    async def create_payment_link(self, amount: int, user_id: int) -> Dict:
        """Создает платежную ссылку"""
        pass
        
    async def check_payment_status(self, payment_id: str) -> Dict:
        """Проверяет статус платежа"""
        pass

class SplitProvider(PaymentProvider):
    BASE_URL = "https://api.split.com/v1"
    
    async def create_payment_link(self, amount: int, user_id: int) -> Dict:
        # Реализация для Split (без KYC)
        pass

class FragmentProvider(PaymentProvider):
    BASE_URL = "https://api.fragment.com/v1"
    
    async def create_payment_link(self, amount: int, user_id: int) -> Dict:
        # Реализация для Fragment (с KYC)
        pass
```

#### 3.2 Обновление main_bot.py
```python
# Новые команды
async def topup_balance(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Пополнение баланса через Split/Fragment"""
    keyboard = [
        [InlineKeyboardButton("💳 Split (без проверки)", callback_data="topup_split")],
        [InlineKeyboardButton("🔐 Fragment (с проверкой)", callback_data="topup_fragment")],
        [InlineKeyboardButton("◀️ Назад", callback_data="main_menu")]
    ]
    
    await update.message.reply_text(
        "💰 Выберите способ пополнения:\n\n"
        "• Split - быстро, без проверки документов\n"
        "• Fragment - для крупных сумм, требует KYC\n\n"
        "💡 Комиссия сервиса: 10%",
        reply_markup=InlineKeyboardMarkup(keyboard)
    )
```

### Этап 4: Система Профилей (2-3 дня)

#### 4.1 Управление Профилями
```python
# user_profiles.py
class AutoPurchaseProfile:
    def __init__(self, user_id: int, db_manager):
        self.user_id = user_id
        self.db = db_manager
        
    async def create_profile(self, settings: Dict) -> Dict:
        """Создает профиль автопокупки"""
        # Валидация настроек
        # Сохранение в БД
        pass
        
    async def update_profile(self, profile_id: int, settings: Dict) -> Dict:
        """Обновляет профиль"""
        pass
        
    async def toggle_profile(self, profile_id: int, is_active: bool) -> Dict:
        """Включает/выключает профиль"""
        pass
```

#### 4.2 Интерфейс Профилей
```python
# Новое меню в боте
async def autopurchase_menu(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Меню автопокупки"""
    keyboard = [
        [InlineKeyboardButton("➕ Создать профиль", callback_data="create_profile")],
        [InlineKeyboardButton("📋 Мои профили", callback_data="my_profiles")],
        [InlineKeyboardButton("📊 История покупок", callback_data="purchase_history")],
        [InlineKeyboardButton("◀️ Назад", callback_data="main_menu")]
    ]
    
    await update.message.reply_text(
        "🤖 Автоматическая покупка подарков\n\n"
        "Создайте профиль с настройками:\n"
        "• Лимиты по звездам (100-1000⭐)\n"
        "• Максимальный тираж\n"
        "• Целевой канал для подарков\n\n"
        "Бот будет автоматически покупать подарки от вашего имени!",
        reply_markup=InlineKeyboardMarkup(keyboard)
    )
```

### Этап 5: Обновление account_manager.py (1-2 дня)

#### 5.1 Новые Методы
```python
class AccountManager:
    async def request_user_permission(self, user_id: int) -> Dict:
        """Запрос разрешения на управление аккаунтом"""
        # Создание мини-приложения для авторизации
        pass
        
    async def buy_gift_as_user(self, user_id: int, gift_id: str, profile_settings: Dict) -> Dict:
        """Покупка подарка от имени пользователя с учетом настроек профиля"""
        # Проверка лимитов профиля
        # Покупка через Pyrogram от имени пользователя
        pass
        
    async def check_user_stars_balance(self, user_id: int) -> int:
        """Проверка баланса Stars у пользователя"""
        # Через Telegram API
        pass
```

### Этап 6: Тестирование (2-3 дня)

#### 6.1 Тестовые Сценарии
- [ ] Пополнение через Split
- [ ] Пополнение через Fragment
- [ ] Создание профиля автопокупки
- [ ] Автоматическая покупка по профилю
- [ ] Проверка комиссий
- [ ] Обработка ошибок

#### 6.2 Нагрузочное Тестирование
- [ ] 100+ одновременных пользователей
- [ ] Обработка webhook'ов
- [ ] Производительность БД

### Этап 7: Миграция Данных (1 день)

#### 7.1 Скрипт Миграции
```python
# migrate_to_new_system.py
async def migrate_users():
    """Миграция существующих пользователей"""
    # Сохранение текущих балансов
    # Создание дефолтных профилей
    # Уведомление пользователей о новой системе
    pass
```

## 📊 ВРЕМЕННАЯ ШКАЛА

| Неделя | Задачи |
|--------|--------|
| 1 | Исследование API, обновление архитектуры |
| 2 | Реализация платежной системы, профили |
| 3 | Интеграция, тестирование |
| 4 | Миграция, запуск |

## ⚠️ РИСКИ И РЕШЕНИЯ

### Риск 1: API недоступны
**Решение**: Использовать альтернативные платежные системы (YooMoney, QIWI)

### Риск 2: Блокировка Telegram
**Решение**: Ограничить частоту запросов, использовать прокси

### Риск 3: Потеря данных
**Решение**: Регулярные бэкапы, постепенная миграция

## 🎯 РЕЗУЛЬТАТ

После миграции получим:
- ✅ Работающую систему пополнения
- ✅ Монетизацию через комиссии (10%)
- ✅ Гибкие профили автопокупки
- ✅ Масштабируемую архитектуру
- ✅ Конкурентоспособный продукт 